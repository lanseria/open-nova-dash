**一、 核心技术选型**

* **界面 UI**：SwiftUI + Combine（轻量响应式界面，代码量极少）
* **网络控制**：原生 `URLSession`（处理 192.168.1.254 的 CGI 指令）
* **视频播放器**：`MobileVLCKit` 或 `KSPlayer`（专门拉取 RTSP 流并开启 iOS 硬件解码）
* **相册管理**：iOS 原生 `Photos` 框架（将从记录仪下载的 MP4/JPG 存入系统相册）

---

**二、 4 大核心功能模块实现**

**1. 连接与心跳管理模块 (Connection & Heartbeat)**

* **局域网感知**：监控 Wi-Fi 状态，识别到网关为 `192.168.1.254` 时自动建立连接。
* **后台心跳包**：启动 Swift `Task` 循环，每 3 秒发起一次 `cmd=3016`，防止记录仪断开。
* **网络防跳动**：配置 `URLSession` 禁用蜂窝网络路由，避免 iOS 因为记录仪 Wi-Fi 无外网而中断请求。

**2. 记录仪控制模块 (Novatek CGI Driver)**

* **实时状态轮询**：定时查询 `cmd=2001`（不带参数，返回录像状态 Value=1/0），实时同步 APP 界面上的录制小红点。注意 `2002` 是"设置视频分辨率"，不是状态查询。
* **一键安全抓拍**：优先直接 `cmd=1001`（本机实测录像中发 1001 也返回成功）；若返回 -22 再走模式切换连招：`切照片模式(3001&par=1) -> 拍照(1001) -> 切回视频(3001&par=0) -> 恢复录像(2001&str=1)`。**不要用 `2001` 停录再拍照**——本机 2001 用 `str=` 传参（`par=` 返回 -22），且状态机敏感（重复停/开返回 -22），恢复录像还会让设备 HTTP 阻塞十几秒。
* **设置项调节**：封装分辨率切换、声音录制开关、格式化 SD 卡等二次确认弹窗。

**3. RTSP 实时画面预览模块 (Live Player)**

* **低延迟拉流**：直接连接 `rtsp://192.168.1.254/novatek/sub`（子码流），延迟可控制在 300ms 以内。
* **画面旋转与比例适配**：自动适应记录仪画面比例，支持一键全屏横屏展示。
* **本地截图/录屏**：除了触发记录仪硬件拍照，APP 端也可直接截取当前播放器画面保存到手机。

**4. SD 卡文件管理与下载模块 (Media Explorer)**

* **文件树拉取**：发送 `cmd=3015` 获取 SD 卡文件列表（本机实测直接返回文件树 XML，无 Status 包裹，按 `<FPATH>` 解析）。实测目录结构：照片 `A:\CARDV\PHOTO\*.JPG`、循环录像 `A:\CARDV\MOVIE\*.TS`，文件名内嵌拍摄时间戳（如 `20260901194216_000002.JPG`），按目录分组即得"循环视频/照片"两个 tab。
* **缩略图预览**：拼接路径 `http://192.168.1.254/A:/CARDV/MOVIE/xxx.TS?custom=1&cmd=4001` 异步加载缩略图（`4001` 待实测）。
* **高速下载与保存**：通过 HTTP 异步拉取原片。⚠️ 循环录像为 **TS 格式**，iOS `PHPhotoLibrary` 不能直接导入：需先用 `FFmpegKit` 无转码重封装（remux）为 MP4 再存入系统相册；在线播放则 `MobileVLCKit`/`KSPlayer` 均支持直接播 TS 流。

---

**三、 界面 UI 布局推荐**

* **主页（预览页）**：上方 16:9 实时 RTSP 视频窗口，下方摆放大尺寸【抓拍】、【开始/停止录像】、【静音开关】和【进入相册】按钮。
* **相册页**：顶部切换【循环视频】 / 【紧急视频】 / 【照片】，网格化展示缩略图，点击支持在线预览播放和批量保存到手机。
* **设置页**：列出固件版本、SD 卡剩余空间，以及【格式化存储卡】操作。