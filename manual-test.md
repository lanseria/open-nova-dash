# 记录仪控制命令人工实测清单

> 目的：逐条验证 停止录像 / 开始录像 / 拍照 链路，用**设备的提示音（叫不叫）+ 屏幕状态**判断固件是否真正执行。
> 在 **Mac 终端**执行（Mac 已连接记录仪 Wi-Fi）。**每条命令之间等 3~5 秒**，不要连发（单线程服务器）。
>
> 响应判读：
> - `<Status>0</Status>` = 成功
> - `<Status>-22</Status>` = 参数/状态不允许（本不该执行的状态，属预期则正常）
> - `<Status>-5</Status>` = 卡满/写卡失败 → 先备份再格式化
> - 卡住十几秒后 `curl (28)` 超时 = 设备在忙，**不要重发**，等 30s 再继续
>
> 判断"是否在录像"的技巧：`2001` 不支持查询，但**录像时剩余空间(3017)会持续减少**——
> 间隔 10 秒查两次，数字在掉 = 录像中。

## A 轮：录像控制（2001）

| # | 目的 | 命令 | 预期 | 响应 | 叫? | 屏幕 |
|---|---|---|---|---|---|---|
| A0 | 确认在线 | `curl -s --max-time 3 "http://192.168.1.254/?custom=1&cmd=3016"` | Status=0 | | | |
| A1 | 记当前空间(基线) | `curl -s --max-time 5 "http://192.168.1.254/?custom=1&cmd=3017"` | Value=字节数 | | | |
| A1b | 10s 后再查一次 | 同上 | 数字在掉=确认录像中 | | | |
| A2 | **停止录像 str=0** | `curl -s --max-time 15 "http://192.168.1.254/?custom=1&cmd=2001&str=0"` | Status=0；设备叫；屏幕录像标停止 | | | |
| A3 | 验证已停(两次间隔10s) | 同 A1 | 两次数字相同=已停 | | | |
| A4 | 参数名对照 par=0 | `curl -s --max-time 15 "http://192.168.1.254/?custom=1&cmd=2001&par=0"` | -22 且**无反应**(证明确实只认 str=) | | | |
| A5 | **开始录像 str=1** | `curl -s --max-time 15 "http://192.168.1.254/?custom=1&cmd=2001&str=1"` | Status=0；设备叫；屏幕恢复录像 | | | |
| A6 | 验证已录(两次间隔10s) | 同 A1 | 数字在掉=已录 | | | |
| A7 | 参数名对照 par=1 | `curl -s --max-time 15 "http://192.168.1.254/?custom=1&cmd=2001&par=1"` | -22 且无反应 | | | |
| A8 | 重复命令对照(再发一次 str=1) | 同 A5 | -22 = 状态机敏感实锤 | | | |

## B 轮：拍照（1001 / 2017，在录像中测）

| # | 目的 | 命令 | 预期 | 响应 | 叫? | 屏幕 |
|---|---|---|---|---|---|---|
| B0 | 确认录像中 | A6 通过即已录像 | — | | | |
| B1 | **直接拍照 1001** | `curl -s --max-time 10 "http://192.168.1.254/?custom=1&cmd=1001"` | Status=0 + `<FPATH>`；快门音；屏幕闪一下 | | | |
| B2 | 录像中抓拍 2017 | `curl -s --max-time 10 "http://192.168.1.254/?custom=1&cmd=2017"` | 上轮 -22，复测看是否依旧 | | | |
| B3 | 验证落卡 | `curl -s --max-time 12 "http://192.168.1.254/?custom=1&cmd=3015" \| grep -io "PHOTO[^<]*" \| tail -5` | PHOTO 目录出现新 .JPG | | | |

## C 轮：模式切换连招（仅当 B1 失败时测）

| # | 目的 | 命令 | 预期 | 响应 | 叫? | 屏幕 |
|---|---|---|---|---|---|---|
| C1 | 切照片模式(会停录像) | `curl -s --max-time 15 "http://192.168.1.254/?custom=1&cmd=3001&par=1"` | Status=0；叫；屏幕切到照片模式 | | | |
| C2 | 拍照 | `curl -s --max-time 10 "http://192.168.1.254/?custom=1&cmd=1001"` | Status=0 + FPATH | | | |
| C3 | 切回视频模式 | `curl -s --max-time 15 "http://192.168.1.254/?custom=1&cmd=3001&par=0"` | Status=0；屏幕回视频模式 | | | |
| C4 | **恢复录像(收尾必做)** | `curl -s --max-time 15 "http://192.168.1.254/?custom=1&cmd=2001&str=1"` | Status=0；屏幕恢复录像 | | | |
| C5 | 验证已录 | 同 A1 两次间隔10s | 数字在掉 | | | |

## 注意事项

- 测试期间若操作间隔超过 30s，先补一条心跳（A0）再继续；Wi-Fi 被踢就重连后从 A0 继续。
- 某条命令超时后：等待设备恢复（反复 A0 直到通），**绝不能立刻重发同一条命令**。
- 全程设备一声不吭但返回 Status=0 → 记录下来，这本身就是关键线索（命令被接受但没执行）。
- 空间低于 0.5GB 时 -5/-22 大概率是卡满导致：先备份，再格式化
  `curl -s --max-time 30 "http://192.168.1.254/?custom=1&cmd=3010&str=1"`（输入即执行，无二次确认，慎用）。

---

# 第二轮：诊断 + 校准（基于第一轮结果修订）

**第一轮结论**：`str=0` 停录成功（秒回 0）；`str=1` 把单线程服务器卡住 >15s（卡仅剩 0.22GB，开始录像要腾挪循环文件）；之后的 par=/1001/2017 全是排队超时，**结果无效**。
真正的"命令不存在"返回 `Status=-256`，不是超时。

**铁律升级**：发完任何 `2001&str=1` 后，必须用下面的"等恢复"命令轮询到设备应答，才能继续下一条：

```bash
# 等恢复（粘贴后它会自己循环，通了打印 OK；超过 ~60s 还不通就 Ctrl-C 等会儿再试）
until curl -s --max-time 3 "http://192.168.1.254/?custom=1&cmd=3016" | grep -q 3016; do sleep 3; done; echo "✅ 设备已恢复"
```

## D 轮：命令支持面诊断（全部只读，最优先跑）

| # | 目的 | 命令 |
|---|---|---|
| D1 | **设备报出支持的全部命令**（一锤定音"命令存不存在"） | `curl -s --max-time 8 "http://192.168.1.254/?custom=1&cmd=3002"` |
| D2 | 当前录像时长（**0=没在录像** → 正规状态查询） | `curl -s --max-time 8 "http://192.168.1.254/?custom=1&cmd=2016"` |
| D3 | 当前工作模式（校准 3001 枚举） | `curl -s --max-time 8 "http://192.168.1.254/?custom=1&cmd=3037"` |
| D4 | 全部设置项当前值 | `curl -s --max-time 8 "http://192.168.1.254/?custom=1&cmd=3014"` |
| D5 | 智阳扩展：硬件识别码 | `curl -s --max-time 8 "http://192.168.1.254/?custom=1&cmd=8567"` |
| D6 | 智阳扩展：支持功能集 | `curl -s --max-time 8 "http://192.168.1.254/?custom=1&cmd=8569"` |

记录：每条的完整响应原文。若返回 `-256` = 本机真不支持该命令；若超时 = 服务器忙，等恢复后再试一次。

## E 轮：模式枚举校准（官方文档 0=照片/1=录像 与本机早期记录相反，必须实测）

| # | 命令 | 观察 |
|---|---|---|
| E0 | D3 (3037) | 记下当前模式值 |
| E1 | `curl -s --max-time 12 "http://192.168.1.254/?custom=1&cmd=3001&par=1"` | 屏幕：进入照片还是录像？ |
| E2 | D3 (3037) + D2 (2016) | 记录模式值；2016 若>0说明在录像 → **par=1=录像模式实锤** |
| E3 | `curl -s --max-time 12 "http://192.168.1.254/?custom=1&cmd=3001&par=0"` | 屏幕：切到了什么模式？ |
| E4 | D3 + D2 | 记录 |
| E5 | 校准完把模式切回录像状态 | 若在照片模式 → `3001&par=1`(录像模式，按 E 轮结论) + `2001&str=1` |

## F 轮：受控复测录像/拍照（每条之间跑一次"等恢复"）

| # | 命令 | 预期 | 叫? | 屏幕 |
|---|---|---|---|---|
| F1 | `curl -s --max-time 20 "http://192.168.1.254/?custom=1&cmd=2001&str=0"` | 0 或 -22；叫 | | |
| F2 | 等恢复 → D2 (2016) | 2016=0 即确认已停 | | |
| F3 | `curl -s --max-time 20 "http://192.168.1.254/?custom=1&cmd=2001&par=0"` | 复测 par：-22 或 0？ | | |
| F4 | 等恢复 → `2001&str=1` | 0；叫；这步超时正常，等恢复即可 | | |
| F5 | 等恢复 → D2 (2016) | 2016>0 且增长 = 确认在录 | | |
| F6 | `curl -s --max-time 10 "http://192.168.1.254/?custom=1&cmd=1001"` | 0+FPATH；快门音 | | |
| F7 | 验证落卡 | `curl -s --max-time 12 "http://192.168.1.254/?custom=1&cmd=3015" \| grep -io "PHOTO[^<]*" \| tail -5` | | |

## G 轮：格式化（**先备份！** 官方参数 par=1；旧写法 str=1 疑似从未生效）

> 备份方式：`uv run script.py album --download 关键字`，或浏览器直接打开文件 URL 保存。
> 卡里 0.22GB 空间是本轮诸多异常的头号嫌疑，G 轮建议放在 D~F 轮之后再做（先拿到诊断数据）。

| # | 命令 | 预期 |
|---|---|---|
| G1 | `curl -s --max-time 30 "http://192.168.1.254/?custom=1&cmd=3010&par=1"` | Status=0，设备提示格式化中/叫 |
| G2 | 等恢复（可能较久）→ A1 (3017) | 空间接近卡总容量 = **格式化真生效**（对照：旧的 str=1 写法） |
| G3 | 格式化后重跑 F 轮 | 复测 2001/1001 是否恢复正常节奏（秒回、有提示音） |
