# 联咏 Novatek 行车记录仪 Wi-Fi 协议文档（当前硬件实测版）

> 适配硬件：**860N72（固件 SF20200714，HTTP 服务 hfs/1.00.000）**。
> 本文档按本仓库 iOS 客户端的四个功能页面组织，每条接口标注当前硬件的实测状态：
> ✅ 已实测可用 · ⚠️ 已接入待复测 · ❌ 本机不支持 · ❓ 社区文档有载但未验证。
> 实测过程与取舍记录见 [iOS.md](iOS.md)，接口的脚本实现见 [script.py](script.py) + [nova_dash/](nova_dash/)。

## 通信基准

- **请求**：`http://192.168.1.254/?custom=1&cmd=[CMD]&par=[PAR]`（字符串参数用 `str=`；本机 `2001` 只认 `str=`，用 `par=` 返回 -22）
- **响应**：`<Function><Cmd>CMD</Cmd><Status>0</Status><Value>VAL</Value></Function>`，`Status=0` 表示成功，负数即 Linux errno
- **本机特例**：`3015` 直接返回文件树 XML（无 Status 包裹），按 `<FPATH>` 判定成败

**错误码（官方表 + 本机实测）**

| Status | 官方名称 | 含义 |
| --- | --- | --- |
| 0 | OK | 成功 |
| -1 | NOFILE | 无文件 |
| -3 | NOBUF | 无缓冲区 |
| -5 | FILE_ERROR | 文件错误/写卡失败：卡满或卡故障 |
| -7 | MOVIE_FULL | 录像中存储满 |
| -8 | MOVIE_WR_ERROR | 录像中写卡出错 |
| -9 | MOVIE_SLOW | 录像中卡速过慢 |
| -11 | STORAGE_FULL | 存储满 |
| -13 | FAIL | 执行失败（抓拍等） |
| -21 | PAR_ERR | 命令参数错误 |
| **-256** | **CMD_NOTFOUND** | **命令不存在**（真"没找到"是这个码，不是超时） |

**诊断命令（3002 已确认本机支持面，2026-09 实测）**

本机 `3002` 实际返回的支持列表（共 68 条）：
`1001 1002 1003` · `2001 2002 2003 2004 2005 2007 2008 2009 2011 2012 2013 2014 2015 2016 2017 2018 2019 2020 2021 2022 2023 2024 2025` · `3001 3002 3003 3004 3005 3006 3007 3008 3009 3010 3011 3012 3013 3014 3015 3016 3017 3018 3019 3021 3022 3023 3024 3025 3026 3029 3030 3031 3032 3033 3034 3037 3038` · `4001 4002 4003 4004 4005` · `5001` · `8005 8020`

关键推论：
- **2016 / 3037 均在列表** → 录像状态与工作模式都有正规查询手段，"靠命令反馈推断状态"的权宜方案可退役
- **2017 / 2018 均在列表** → 抓拍命令存在，此前 -22 是时机/状态问题（空按），录像中复测
- **3020 不在列表** → 本机无 TCP 3333 状态推送；原版 App 的提示音应为设备本地按键音
- **8005 / 8020** 为厂商自定义命令，用途未知

| cmd | 说明 | 状态 |
| --- | --- | --- |
| 3002 | 返回支持的全部命令列表 | ✅ 实测可用（即上方清单） |
| 2016 | 当前录像时长（秒），**返回 0 = 没在录像** → 正规的录像状态查询 | ✅ 已确认支持，值待实测 |
| 3037 | 查询当前工作模式（校准 3001 枚举用） | ✅ 已确认支持，值待实测 |
| 3014 | 返回全部菜单设置当前值 | ✅ 已确认支持 |
| 3031&str=all | 返回全部菜单项及可选项 | ✅ 已确认支持 |
| 3020 (socket 3333) | 设备主动推送状态 | ❌ 不在支持列表，本机无此通道 |

**模式映射（2026-09-17 实测定稿，3037 值为设备回报）**

| 3001 参数 | 3037 回报 | 模式 | 证据 |
| --- | --- | --- | --- |
| `par=1` | **1** | 录像 | 录像中 3037=1 且 2016 持续增长；par=1 切换后值不变 |
| `par=2` | **3** | 回放 | par=2 切换后 3037=3 |
| `par=0` | **4**（待屏幕最终确认） | 照片（推断） | par=0 切换后 3037=4；排除录像(1)/回放(3)，照片为唯一剩余主模式 |

结论：**readme 早期记录"0=视频 1=拍照"是错的**，官方枚举方向正确但设备 3037 回报用内部编号（1/3/4）。
**历史拍照连招失败根因**：旧代码把 par=1 当"照片模式"，实际切到录像模式——整条连招在错误模式下运行。

**录像控制（2026-09-17 实测定稿）**

- `2001&str=1` 开始录像：✅ 实测 2 秒返回 Status=0，设备发出"开始录像"提示音（此前 15s 超时为刚停录后固件收尾 + 卡满腾挪循环文件所致，命令本身成功）
- `2001&str=0` 停止录像：✅ 实测秒回 Status=0
- `2016` 录像状态：✅ 返回当前片段秒数（0=未录像），间隔查询数字增长即确认在录
- 节奏红线：**停录后立即开录会长时间阻塞**（文件收尾 + 卡满时腾挪最旧循环文件），停→开之间须等设备恢复（3016 轮询）

## 架构红线（驱动层必须遵守，全部为本机实测教训）

1. **HTTP 服务器是单线程的，所有请求必须串行**（含心跳），并发会导致连接被重置/服务长时间无响应。
2. **心跳 `cmd=3016` 必须每 3~5 秒一次**，否则设备主动断开 Wi-Fi。
3. **读超时 ≠ 命令失败**：重命令后设备会阻塞几秒到几十秒。严禁超时后自动重发状态命令（请求会堆积且可能重复执行），只允许重试"连接建立失败"（请求未送达，安全）。
4. **心跳失败要退避降频**：设备无响应时把心跳降到 15s 间隔，恢复后回到正常节奏。
5. **`2001` 状态机敏感**：`str=0` 只在录像中有效、`str=1` 只在空闲时有效，重复同状态命令返回 -22。

---

## 一、连接与心跳（对应 iOS 连接页）

脚本入口：`uv run script.py connect`

| cmd | 参数 | 状态 | 说明 |
| --- | --- | --- | --- |
| 3016 | — | ✅ | 心跳保活，必须每 3~5 秒一次，否则设备断开 Wi-Fi |

**连接策略（iOS / 脚本一致）**：手动触发连接 → 连续 ping 最多 3 次 → 成功后启动 3s 心跳；连续 3 次心跳失败判定断开，回到待连接页并停止一切后台请求。

## 二、状态仪表盘（对应 iOS 状态页）

脚本入口：`uv run script.py status`（全部只读查询）

| cmd | 参数 | 状态 | 说明 |
| --- | --- | --- | --- |
| 3012 | — | ✅ | 设备信息/固件版本，内容在 `<String>` 节点（本机返回 SF20200714） |
| 3017 | — | ✅ | SD 卡剩余空间（字节）。本机卡满时 0.2GB 会导致拍照 -5 / 录像 -22 |
| 3019 | — | ✅ | 电池状态：0=满 1=中 2=低 3=耗尽 5=充电中 |
| 3024 | — | ✅ | SD 卡状态：0=无卡 1=正常 2=被锁定 |
| 3014 | — | ❓ | 查询全部配置 |
| 3011 | — | ❓ | ⚠️ 恢复出厂设置（危险命令，会重置全部设置） |

## 三、相册与文件（对应 iOS 相册页）

脚本入口：`uv run script.py album`、`uv run script.py album --download 关键字`

| cmd/URL | 参数 | 状态 | 说明 |
| --- | --- | --- | --- |
| 3015 | — | ✅ | 文件列表，**直接返回文件树 XML**（无 Status 包裹，按 `<FPATH>` 解析）。若固件拒绝则回退 `3001&par=2` 回放模式流程 |
| HTTP 下载 | — | ✅ | 见下方 URL 规则，实测 200（iOS 相册在线播放与下载均基于此） |
| 4001 | 拼在文件 URL 后 | ❓ | 获取文件缩略图，待实测（iOS 未采用，封面用 FFmpeg 抽帧） |
| 4003 | `str=[filePath]` | ❓ | 删除特定文件，待实测 |

**目录结构（本机实测）**：照片 `A:\CARDV\PHOTO\*.JPG`、循环录像 `A:\CARDV\MOVIE\*.TS`、视频伴生同名 `.THM` 缩略图文件；文件名内嵌拍摄时间戳（如 `20260901194216_000002.TS`）。

**下载 URL 规则（✅ 实测可用）**：去掉 `3015` 返回路径的 `A:` 盘符前缀，即

```
A:\CARDV\MOVIE\x.TS  →  http://192.168.1.254/CARDV/MOVIE/x.TS
```

服务器为 `hfs/1.00.000`，响应带 `Connection: close`（不复用连接）。

## 四、控制命令（对应 iOS 控制页）

脚本入口：`uv run script.py control --capture | --record on|off | --format-sd`

| cmd | 参数 | 状态 | 说明 |
| --- | --- | --- | --- |
| 3001 | `par=0/1/2` | ✅ | 切换工作模式。**枚举两说冲突（官方 0=照片/1=录像 vs 本机早期记录 0=视频/1=拍照），以 3037 校准为准**。本机切模式后不会自动恢复录像，必须收尾补 `2001&str=1` |
| 2016 | — | ✅ 支持 | 查询当前录像时长（0=没在录像），录像状态查询的正规手段，值待实测 |
| 3037 | — | ✅ 支持 | 查询当前工作模式，**用它的实测值定枚举** |
| 2015 | `par=1/0` | ✅ 支持 | 开/关实时画面（live view），与 RTSP 拉流配套 |
| 1001 | — | ✅ | 拍照。本机录像中直接发也实测成功；-5 = 卡满写卡失败 |
| 2017 + 2018 | — | ✅ 支持 | 抓拍两步：2017 拍（-13=执行失败）→ 2018 保存（-1=无文件）；此前 -22 为空按，录像中复测 |
| 2001 | `str=1/0` | ✅ | 开始/停止录像（**str=0 秒回成功已实测；str=1 会长时间阻塞服务器，卡快满时更甚**）。状态机敏感（重复同状态 -22） |
| 2001 | `par=1/0` | ⚠️ | 官方/nutsey 表的标准写法。本机早期实测 -22，但当时正值卡满污染，**待格式化后复测** |
| 2001 | 无参数 | ❌ | 查询录像状态，本机返回 -21（查询变体不支持，用 2016 替代） |
| 3010 | **`par=1`** | ⚠️ | 格式化 SD 卡。**官方参数为 par（par=1 格卡，par=0 格 flash）；此前一直用 str=1 疑似从未生效——卡反复满与此吻合，待复测**（危险操作） |
| 3005 / 3006 | `str=YY-MM-DD` / `str=hh-mm-ss` | ✅ 支持 | 同步日期/时间（官方用连字符） |
| 2002 / 1002 | `par=[枚举]` | ✅ 支持 | 设置视频/照片分辨率 |
| 1003 / 2009 | — | ✅ 支持 | 剩余可拍张数 / 当前码率下最大录像时长 |
| 4001 / 4002 | 文件路径（**大写**） | ✅ 支持 | 缩略图 / 预览图（需回放模式；iOS 现用 .THM 方案可作备选） |
| 4003 / 4004 | 文件路径 | ✅ 支持 | 删除单文件 / 删除全部（相册删除功能的候选） |
| 4005 | — | ✅ 支持 | 获取录像文件信息 |

**拍照安全连招（✅ 实测，iOS 与脚本一致）**：

```
1001 直接拍照
  └─ 失败(-22) → 3001&par=1 照片模式 → 1001 拍照 → 3001&par=0 视频模式 → 2001&str=1 恢复录像
```

**不要用 `2001` 停录再拍照**：本机 `2001` 状态机敏感，恢复录像还会让设备 HTTP 阻塞十几秒。

## 五、实时视频流（对应 iOS 控制页直播）

脚本入口：`uv run script.py control --live`

| 节点 | 说明 | 状态 |
| --- | --- | --- |
| `rtsp://192.168.1.254/novatek/main` | 主码流（高清 1080P/4K） | ✅ |
| `rtsp://192.168.1.254/novatek/sub` | 子码流（标清/低延迟） | ✅ |
| `http://192.168.1.254:8080/live` | HTTP-FLV 预览流（部分型号） | ❓ |

iOS 端用 KSPlayer/FFmpeg 直接拉流（硬解），终端可用 `ffplay -fflags nobuffer rtsp://192.168.1.254/novatek/sub` 验证。

---

## 六、固件底层架构（社区资料，未实测）

如果你计划做深度系统重构或串口拆机编译，联咏的嵌入式系统具有以下技术特征：

* **操作系统架构**：双核/单核 Linux Kernel (通常为 Linux 3.10 / 4.9) + eCos RTOS 混合架构。
* **交叉编译工具链**：`arm-ca9-linux-gnueabihf-` 或 `arm-linux-uclibcgnueabihf-`。
* **硬件抽象层 (HDAL)**：联咏的核心视频处理库（媒体处理框架，类似于海思的 MPP），负责 Sensor 图像输入（VI）、视频编码（VENC，H.264/H.265）以及图像算法处理。
* **固件打包格式**：升级文件通常为 `FW966X.bin`，可使用 `binwalk -e` 解包，内部包含 `u-boot.bin`、`uImage` (Kernel) 以及 `rootfs.squashfs`。

## 七、社区开源逆向工程参考

可以在 GitHub 上检索以下项目获取更详尽的源代码实现与逆向成果：

* **nutsey/novatek-web-api-commands**：NT96650/96655/96660 通用命令总表（本文档 2xxx/3xxx 命令的社区权威来源）。
* **GoXtreme-Wi-Fi-API**：完整的 Novatek 动作相机 API 映射表。
* **rtsp-stream-converter**：专为联咏芯片打造的 RTSP 实时抓帧与转发中间件。
* **智阳开放文档（Sunningsoft）**：官方风格协议文档，含错误码表、菜单查询（3014/3031）、智阳扩展命令（8567/8569）与 TCP 3333 状态推送说明。
