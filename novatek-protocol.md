# 联咏 Novatek 行车记录仪 Wi-Fi 协议文档（当前硬件实测版）

> 适配硬件：**860N72（固件 SF20200714，HTTP 服务 hfs/1.00.000）**。
> 本文档按本仓库 iOS 客户端的四个功能页面组织，每条接口标注当前硬件的实测状态：
> ✅ 已实测可用 · ⚠️ 已接入待复测 · ❌ 本机不支持 · ❓ 社区文档有载但未验证。
> 实测过程与取舍记录见 [iOS.md](iOS.md)，接口的脚本实现见 [script.py](script.py) + [nova_dash/](nova_dash/)。

## 通信基准

- **请求**：`http://192.168.1.254/?custom=1&cmd=[CMD]&par=[PAR]`（字符串参数用 `str=`；本机 `2001` 只认 `str=`，用 `par=` 返回 -22）
- **响应**：`<Function><Cmd>CMD</Cmd><Status>0</Status><Value>VAL</Value></Function>`，`Status=0` 表示成功，负数即 Linux errno
- **本机特例**：`3015` 直接返回文件树 XML（无 Status 包裹），按 `<FPATH>` 判定成败

**错误码（本机实测含义）**

| Status | 含义 |
| --- | --- |
| -1 | 不支持/被拒绝 |
| -3 | 存储或状态错误：录像中查文件列表、SD 卡未就绪时常见 |
| -5 | EIO 写卡失败：卡满或卡故障（备份后格式化） |
| -13 | 抓拍执行失败 |
| -21 | 查询变体不支持（本机 `2001` 无参查询实测返回） |
| -22 | EINVAL：参数名/值不对，或当前状态不允许（重复停/开录像） |

## 架构红线（驱动层必须遵守，全部为本机实测教训）

1. **HTTP 服务器是单线程的，所有请求必须串行**（含心跳），并发会导致连接被重置/服务长时间无响应。
2. **心跳 `cmd=3016` 必须每 3~5 秒一次**，否则设备主动断开 Wi-Fi。
3. **读超时 ≠ 命令失败**：重命令后设备会阻塞几秒到几十秒。严禁超时后自动重发状态命令（请求会堆积且可能重复执行），只允许重试"连接建立失败"（请求未送达，安全）。
4. **心跳失败要退避降频**：设备无响应时把心跳降到 15s 间隔，恢复后回到正常节奏。
5. **`2001` 状态机敏感**：`str=0` 只在录像中有效、`str=1` 只在空闲时有效，重复同状态命令返回 -22。

---

## 一、连接与心跳（对应 iOS 连接页）

脚本入口：`uv run script.py connect`

| cmd | 参数 | 状态 | 说明 |
| --- | --- | --- | --- |
| 3016 | — | ✅ | 心跳保活，必须每 3~5 秒一次，否则设备断开 Wi-Fi |

**连接策略（iOS / 脚本一致）**：手动触发连接 → 连续 ping 最多 3 次 → 成功后启动 3s 心跳；连续 3 次心跳失败判定断开，回到待连接页并停止一切后台请求。

## 二、状态仪表盘（对应 iOS 状态页）

脚本入口：`uv run script.py status`（全部只读查询）

| cmd | 参数 | 状态 | 说明 |
| --- | --- | --- | --- |
| 3012 | — | ✅ | 设备信息/固件版本，内容在 `<String>` 节点（本机返回 SF20200714） |
| 3017 | — | ✅ | SD 卡剩余空间（字节）。本机卡满时 0.2GB 会导致拍照 -5 / 录像 -22 |
| 3019 | — | ✅ | 电池状态：0=满 1=中 2=低 3=耗尽 5=充电中 |
| 3024 | — | ✅ | SD 卡状态：0=无卡 1=正常 2=被锁定 |
| 3014 | — | ❓ | 查询全部配置 |
| 3011 | — | ❓ | ⚠️ 恢复出厂设置（危险命令，会重置全部设置） |

## 三、相册与文件（对应 iOS 相册页）

脚本入口：`uv run script.py album`、`uv run script.py album --download 关键字`

| cmd/URL | 参数 | 状态 | 说明 |
| --- | --- | --- | --- |
| 3015 | — | ✅ | 文件列表，**直接返回文件树 XML**（无 Status 包裹，按 `<FPATH>` 解析）。若固件拒绝则回退 `3001&par=2` 回放模式流程 |
| HTTP 下载 | — | ✅ | 见下方 URL 规则，实测 200（iOS 相册在线播放与下载均基于此） |
| 4001 | 拼在文件 URL 后 | ❓ | 获取文件缩略图，待实测（iOS 未采用，封面用 FFmpeg 抽帧） |
| 4003 | `str=[filePath]` | ❓ | 删除特定文件，待实测 |

**目录结构（本机实测）**：照片 `A:\CARDV\PHOTO\*.JPG`、循环录像 `A:\CARDV\MOVIE\*.TS`、视频伴生同名 `.THM` 缩略图文件；文件名内嵌拍摄时间戳（如 `20260901194216_000002.TS`）。

**下载 URL 规则（✅ 实测可用）**：去掉 `3015` 返回路径的 `A:` 盘符前缀，即

```
A:\CARDV\MOVIE\x.TS  →  http://192.168.1.254/CARDV/MOVIE/x.TS
```

服务器为 `hfs/1.00.000`，响应带 `Connection: close`（不复用连接）。

## 四、控制命令（对应 iOS 控制页）

脚本入口：`uv run script.py control --capture | --record on|off | --format-sd`

| cmd | 参数 | 状态 | 说明 |
| --- | --- | --- | --- |
| 3001 | `par=0/1/2` | ✅ | 切换工作模式：0=视频 1=拍照 2=回放。**本机切模式后不会自动恢复录像，必须收尾补 `2001&str=1`** |
| 1001 | — | ✅ | 拍照。本机录像中直接发也实测成功；-5 = 卡满写卡失败 |
| 2001 | `str=1/0` | ✅ | 开始/停止录像。状态机敏感（重复同状态 -22）；执行后设备可能阻塞十几秒 |
| 2001 | 无参数 | ❌ | 查询录像状态，本机返回 -21（不支持查询变体，状态只能靠命令反馈推断） |
| 2001 | `par=` | ❌ | 参数名不对，本机只认 `str=`，用 `par=` 返回 -22 |
| 2017 | — | ❌ | 录像中抓拍，本机返回 -22（抓拍走 1001） |
| 3010 | `str=1` | ⚠️ | 格式化 SD 卡（危险操作；脚本与 iOS 均已接入，需显式确认） |
| 3005 / 3006 | `str=YYYY-MM-DD` / `str=hh:mm:ss` | ❓ | 同步日期/时间 |
| 2002 / 1002 | `par=[枚举]` | ❓ | 设置视频/照片分辨率 |
| 1003 / 2009 | — | ❓ | 查询剩余可拍张数 / 本次最大录像时长 |

**拍照安全连招（✅ 实测，iOS 与脚本一致）**：

```
1001 直接拍照
  └─ 失败(-22) → 3001&par=1 照片模式 → 1001 拍照 → 3001&par=0 视频模式 → 2001&str=1 恢复录像
```

**不要用 `2001` 停录再拍照**：本机 `2001` 状态机敏感，恢复录像还会让设备 HTTP 阻塞十几秒。

## 五、实时视频流（对应 iOS 控制页直播）

脚本入口：`uv run script.py control --live`

| 节点 | 说明 | 状态 |
| --- | --- | --- |
| `rtsp://192.168.1.254/novatek/main` | 主码流（高清 1080P/4K） | ✅ |
| `rtsp://192.168.1.254/novatek/sub` | 子码流（标清/低延迟） | ✅ |
| `http://192.168.1.254:8080/live` | HTTP-FLV 预览流（部分型号） | ❓ |

iOS 端用 KSPlayer/FFmpeg 直接拉流（硬解），终端可用 `ffplay -fflags nobuffer rtsp://192.168.1.254/novatek/sub` 验证。

---

## 六、固件底层架构（社区资料，未实测）

如果你计划做深度系统重构或串口拆机编译，联咏的嵌入式系统具有以下技术特征：

* **操作系统架构**：双核/单核 Linux Kernel (通常为 Linux 3.10 / 4.9) + eCos RTOS 混合架构。
* **交叉编译工具链**：`arm-ca9-linux-gnueabihf-` 或 `arm-linux-uclibcgnueabihf-`。
* **硬件抽象层 (HDAL)**：联咏的核心视频处理库（媒体处理框架，类似于海思的 MPP），负责 Sensor 图像输入（VI）、视频编码（VENC，H.264/H.265）以及图像算法处理。
* **固件打包格式**：升级文件通常为 `FW966X.bin`，可使用 `binwalk -e` 解包，内部包含 `u-boot.bin`、`uImage` (Kernel) 以及 `rootfs.squashfs`。

## 七、社区开源逆向工程参考

可以在 GitHub 上检索以下项目获取更详尽的源代码实现与逆向成果：

* **GoXtreme-Wi-Fi-API**：完整的 Novatek 动作相机 API 映射表。
* **rtsp-stream-converter**：专为联咏芯片打造的 RTSP 实时抓帧与转发中间件。
